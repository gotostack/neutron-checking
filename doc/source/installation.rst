============
Installation
============

At the command line::

    $ pip install neutron-checking

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv neutron-checking
    $ pip install neutron-checking
